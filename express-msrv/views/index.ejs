<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="id" type="text" class="ghost-input" placeholder="Ingrese un id" required>
          <input type="submit" class="ghost-button" value="Obtener factura">
        </form>
        <% if(locals.response !== null){ %>
          <p><%= locals.response %></p>
        <% } %>

        <% if(locals.error !== null){ %>
          <p><%= locals.error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
